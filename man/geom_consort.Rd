% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_consort.R, R/geom_consort_arrow.R,
%   R/geom_consort_box.R, R/geom_consort_diagram.R, R/geom_consort_line.R
\name{geom_consort}
\alias{geom_consort}
\alias{geom_consort_arrow}
\alias{geom_consort_box}
\alias{GeomConsortDiagram}
\alias{geom_consort_line}
\title{CONSORT diagram layers for ggplot2}
\usage{
geom_consort(
  label_color = "black",
  label_size = 11,
  label_height = 1,
  fill = "white",
  box_color = "black"
)

geom_consort_arrow(...)

geom_consort_box(label_color = "black", label_size = 11, label_height = 1, ...)

geom_consort_line(x, xend, y, yend, ...)
}
\arguments{
\item{label_color}{Color of the box label text.}

\item{label_size}{Size of the box label text, in points.}

\item{label_height}{Line height of the box label text.}

\item{fill}{Fill color of the boxes.}

\item{box_color}{Color of the box borders.}

\item{...}{For \code{geom_consort_box()}, additional arguments passed to
\code{\link[ggtext:geom_richtext]{ggtext::geom_richtext()}}. Ignored by \code{geom_consort_arrow()}.}

\item{x, xend, y, yend}{Coordinates of the line segment drawn by
\code{geom_consort_line()}.}
}
\value{
A ggplot2 layer or list of layers that can be added to a plot.
}
\description{
\code{geom_consort()} draws the boxes, arrows, and lines of a \code{ggconsort} object
created with \code{\link[=consort_box_add]{consort_box_add()}}, \code{\link[=consort_arrow_add]{consort_arrow_add()}}, and
\code{\link[=consort_line_add]{consort_line_add()}}. It measures the rendered size of each box at draw
time, so boxes are centered on their (\code{x}, \code{y}) coordinates and arrows
start and end exactly at box edges, whatever the device size.
\code{geom_consort_arrow()} and \code{geom_consort_box()} are the legacy component
layers, which anchor arrows at box centers and shift box text by its
justification; they remain available for diagrams that depend on that
behavior. \code{geom_consort_line()} draws a standalone line segment with the
same styling as the diagram lines.
}
\examples{
cohorts <- trial_data \%>\%
  cohort_start("Assessed for eligibility") \%>\%
  cohort_define(
    randomized = .full \%>\% dplyr::filter(declined != 1)
  ) \%>\%
  cohort_label(randomized = "Randomized")

consort <- cohorts \%>\%
  consort_box_add("full", 0, 10, cohort_count_adorn(cohorts, .full)) \%>\%
  consort_box_add("randomized", 0, 0, cohort_count_adorn(cohorts, randomized)) \%>\%
  consort_arrow_add("full", "bottom", "randomized", "top")

library(ggplot2)
ggplot(consort) +
  geom_consort() +
  theme_consort(margin_h = 12, margin_v = 5)
}
