# Initialize a ggconsort object for plotting. Used internally.

Initialize a ggconsort object for plotting. Used internally.

## Usage

``` r
consort_start(.data)
```

## Arguments

- .data:

  A `ggconsort_cohort` object
